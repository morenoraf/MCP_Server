8 שיעור מטלה - סגל יורם ד"ר MCPArchitecture Assignment

8 שיעור - מטלה

סגל יורם ד"ר

AI סוכני ארכיטקטורת עם מערכת בניית
MCP Server מבוססת

מבוא 1

תוכנה מערכת בניית תוך בשיעור, שנלמדו הארכיטקטורה עקרונות את ליישם היא המטלה מטרת
ומקצועית. מודולרית

יצירתי חופש

הדרישות את ולפרש שלו הפרויקט את לבחור רשאי הסטודנט פתוחה. בצורה מוגדרת המטלה
שנלמדו. הארכיטקטורה עקרונות את מדגים הוא עוד כל הבנתו, לפי

הפרויקט נושא 2

חשבוניות מערכת מומלצת: אפשרות 1.2

הכוללת: חשבוניות להכנת מערכת

לקוחות ניהול •
קבלה( עסקה, חשבונית מס, )חשבונית חשבוניות הפקת •

סידוריים מספרים ניהול •
מע"מ חישובי •

חשבוניות שליחת •

חלופית אפשרות 2.2

עקרונות כל של הדגמה מאפשר שהפרויקט בתנאי העדפתו, לפי אחר פרויקט לבחור רשאי הסטודנט
הנדרשים. הארכיטקטורה

השלבים חמשת 3

נוספים. רכיבים ומוסיף הקודם על מבוסס שלב כל מתפתחים. שלבים בחמישה הפרויקט את לבנות יש

1



8 שיעור מטלה - סגל יורם ד"ר MCPArchitecture Assignment

בסיסיות תשתיות :1 שלב 1.3

והשיתוף התשתיות שכבת הקמת מטרה:
לדוגמה: רכיבים

)Configuration( קונפיגורציה שכבת •
)Logging( לוגים מנגנון •

)Exception Handling( בשגיאות טיפול •
הפרויקט תיקיות מבנה •
בסיסיות יחידה בדיקות •

שבחר. למערכת נדרשות תשתיות אילו יגדיר הסטודנט לפרשנות: פתוח

כלים עם MCP שרת :2 שלב 2.3

Tools עם בסיסי MCP שרת בניית מטרה:
לדוגמה: רכיבים

JSON וסכמת תיאור מזהה, עם )Tools( כלים הגדרת •
בסיסית עסקית לוגיקה •
נתונים לאחסון חיבור •

שלהם. הפונקציונליות תהיה ומה לממש כלים אילו יבחר הסטודנט לפרשנות: פתוח

הפרימיטיבים שלושת :3 שלב 3.3

MCP של הפרימיטיבים בכל לתמיכה המערכת הרחבת מטרה:
רכיבים:

)Write( מצב שמשנות פעולות - Tools •
ודינמיים סטטיים - )Read( נתונים קריאת - Resources •

המודל להדרכת תבניות - Prompts •
שלו. למערכת נדרשים ותבניות משאבים אילו יגדיר הסטודנט לפרשנות: פתוח

2



8 שיעור מטלה - סגל יורם ד"ר MCPArchitecture Assignment

תקשורת שכבת :4 שלב 4.3

מודולרית תקשורת שכבת הוספת מטרה:
לדוגמה: רכיבים

מהלוגיקה התקשורת שכבת הפרדת •
)HTTP/SSE ,STDIO )למשל: אחד תקשורת סוג בלפחות תמיכה •

הקוד שינוי ללא התקשורת שכבת את להחליף יכולת •
שלו. לפרויקט המתאימה התקשורת שיטת את יבחר הסטודנט לפרשנות: פתוח

משתמש וממשק SDK :5 שלב 5.3

משתמש ממשק סוגי שני ולפחות SDK שכבת הוספת מטרה:
רכיבים:

דרכה עוברות הפעולות כל - SDK שכבת •
)לבחירה(: GUI סוגי שני לפחות •

)CLI( טרמינל ממשק –

דסקטופ אפליקציית –

)Web( ווב ממשק –

אחר –

המשתמש. חוויית תהיה ומה לממש ממשקים אילו יבחר הסטודנט לפרשנות: פתוח

נדרשים ארכיטקטורה עקרונות 4

הפרויקט: כל לאורך הבאים העקרונות על להקפיד יש

מקונפיגורציה מגיעים הערכים כל - )No Hard Coding( קבועים ערכים אפס .1

המערכת שאר שינוי ללא להחלפה ניתן רכיב כל - מודולריות .2

לקובץ שורות 150 עד מומלץ - קצרים קבצים .3

קלאס או בפונקציה להיות צריך עצמו על שחוזר קוד - קוד שכפול אפס .4

הקוד לארגון בקלאסים שימוש - )OOP( עצמים מונחית עבודה .5

רכיב לכל בדיקות - )Unit Tests( יחידה בדיקות .6

בשגיאות וטיפול חוזר ניסיון נעילה, מנגנוני - במשאבים טיפול .7

3



8 שיעור מטלה - סגל יורם ד"ר MCPArchitecture Assignment

הגשה 5

ההגשה מבנה 1.5

השלבים חמשת כל את הכולל אחד Repository •
Repository-ה בתוך נפרד כפרויקט/ענף שלב כל •

לו ומוסיף הקודם על מבוסס שלב כל •

מקבילית לעבודה טיפ

``ענף דפוס עם GitWorktrees-ב להשתמש מומלץ במקביל, AI סוכני מספר עם יעילה לעבודה
נוספים. לפרטים א' נספח ראו סוכן''. לכל

להגיש מה 2.5

מלא מקור קוד •
יחידה בדיקות •

הסטודנט( שיקול )לפי בסיסי תיעוד •

הערכה 6

על: תתבסס ההערכה

שנלמדו הארכיטקטורה עקרונות הדגמת •
שכבות בין והפרדה מודולריות •

והבדיקות הקוד איכות •
השלבים בין ברורה התקדמות •

הערה

ברגע נכונה. ארכיטקטורה ולייצר בפרומפט דרישות לנסח ללמוד היא העיקרית המטרה
גבוהה. במהירות מערכות לייצר אפשר נכונה, שהארכיטקטורה

4



8 שיעור מטלה ­ סגל יורם ד”ר tnemngissA erutcetihcrA PCM

Git Worktrees עם מקבילי פיתוח א׳

במקביל עבודה הבעיה: א׳.1

על לעבוד חכמים סוכנים למספר נאפשר כיצד למורכב: הופך האתגר לתמונה, נכנסים AI כשסוכני
בו-זמנית? קוד אותו

?Worktree זה מה א׳.2

הגדרה

לתיקיות מאגר מאותו ענפים מספר (checkout) לבדוק שמאפשר מנגנון הוא Git Worktree
היסטוריה משתף אך קבצים, מבחינת לחלוטין עצמאי הוא Worktree כל בו-זמנית. נפרדות

האחרים. כל עם ומקור

סוכן'' לכל ``ענף דפוס א׳.3

היתרונות: משלו. Worktree סוכן לכל לתת ממליצה Anthropic

משלו קבצים על עובד סוכן כל --- מלא בידוד •
העבודה את למזג קל --- משותפת היסטוריה •

בו-זמנית עובדים סוכנים --- אמיתית מקבילית עבודה •
רגילות Git פקודות --- פשוט ניהול •

מעשי מדריך א׳.4

Worktrees יצירת :1 שלב א׳.1.4

תריצי Worktrees השולשל םינכוס

# Navigate to main project

cd /path/to/project

# Create worktree for Agent 1: Feature Development

git worktree add ../project­agent1­feature ­b agent1/new­api

# Create worktree for Agent 2: Bug Fix

git worktree add ../project­agent2­bugfix ­b agent2/fix­security

# Create worktree for Agent 3: Documentation

git worktree add ../project­agent3­docs ­b agent3/update­readme

# Verify all worktrees exist

git worktree list

5



8 שיעור מטלה - סגל יורם ד"ר MCPArchitecture Assignment

הסוכנים הפעלת :2 שלב א׳.2.4

ייעודית: משימה עם Claude Code סוכן הפעילו נפרד, טרמינל בכל

תלעפה םינכוס ליבקמב

# Terminal 1: Agent 1 ­ Feature Development

cd ../project­agent1­feature

claude

> "Implement OAuth2 authentication for the API"

# Terminal 2: Agent 2 ­ Bug Fix

cd ../project­agent2­bugfix

claude

> "Fix the SQL injection vulnerability in user login"

# Terminal 3: Agent 3 ­ Documentation

cd ../project­agent3­docs

claude

> "Update README with new API documentation"

העבודה מיזוג :3 שלב א׳.3.4

:main-ל שלו הענף את ממזגים מסיים, כשסוכן

גוזימ תדובע םינכוסה

# Return to main project

cd /path/to/project

# Merge Agent 1's feature branch

git merge agent1/new­api ­m "Feature: OAuth2 authentication"

# Merge Agent 2's bugfix branch

git merge agent2/fix­security ­m "Fix: SQL injection

vulnerability"

# Clean up worktrees

git worktree remove ../project­agent1­feature

git worktree remove ../project­agent2­bugfix

מתקדמים טיפים א׳.5

agent2/bugfix­security ,agent1/feature­oauth תיאוריים: ענפים שמות •
הענף לעדכון git rebase origin/main-ב השתמשו סנכרון: •

מיזוג לפני לבדיקה git merge –no­commit –no­ff קונפליקטים: בדיקת •

6



8 שיעור מטלה - סגל יורם ד"ר MCPArchitecture Assignment

חכמים. סוכנים עם במקביל עבודה הוא תוכנה פיתוח של העתיד

סגל יורם לד"ר שמורות הזכויות כל ©

7